<h1> Story Index </h1>
<p>
  <% @stories.each do |s| %>
    <p>
    <%= link_to "#{s.title}", story_path(id: s.id) %>
    </p>
  <% end %>
</p>

<br>

<p>
  <%= link_to 'New Story', new_story_path %>
</p>
